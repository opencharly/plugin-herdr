# 2026.245.1008 — skill: fix the herdr-box cross-reference target (docs gate: unresolvable corpus ref)

## Summary

The `herdr` skill entity's cross-reference pointed at `/charly-pod:herdr`; the
box skill is actually `herdr-box` (automation family). The docs regeneration
gate rejects unresolvable corpus references — the cross-ref now resolves to
`/charly-automation:herdr-box`.

## How tested

```
$ charly box validate
charly box validate: OK — ... 0 warnings, 0 errors
```

The resolution is verified by the docs generator on the corpus side (the gate
that flagged it) after this tags + the marketplace re-pin. Diff: 1 line
(`charly.yml`).

## Rulebook compliance

- **R1** — surfaced by the docs regen gate; fixed at the source.
- **PR-only landing** — branch-per-change; diff verified against origin/main.

## Change classification

- Change class: fix (skill corpus cross-reference)
- Verification gate: `box validate`; docs regen after re-pin
- Attribution tier: `analysed on a live system`

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*